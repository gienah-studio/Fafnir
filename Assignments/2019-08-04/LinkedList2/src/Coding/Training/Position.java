package Coding.Training;

public interface Position {
    public Object getElement(); // Return the element on current position
    public Object setElement(Object e); // Save the given element to current position, return the replaced one

}
